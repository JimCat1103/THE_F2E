import React from 'react';
import {pure} from "recompose";
import {Style} from "./style";

const ComicOpiton = pure(({

                       }) => {
    return <Style>

    </Style>
});

export default ComicOpiton;

ComicOpiton.propTypes = {
};
